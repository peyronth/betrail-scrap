name: Build App
on:
  push:
    branches:
      - master

jobs:
  build-app:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build App
      run: pyinstaller main.spec
